x-common: &common
  build:
    context: .
    dockerfile: Dockerfile
  volumes:
    - ~/.nanobot:/root/.nanobot
  command: ["gateway"]
  restart: unless-stopped
  ports:
    - 18790:18790

services:
  # Slim: Twilio only (no Node.js)
  nanobot-twilio:
    <<: *common
    profiles:
      - twilio
    build:
      context: .
      args:
        EXTRAS: twilio
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 512M
        reservations:
          cpus: '0.25'
          memory: 128M

  # Twilio + Claude agent engine
  nanobot-twilio-claude:
    <<: *common
    command: ["gateway", "--verbose"]
    profiles:
      - twilio-claude
    build:
      context: .
      args:
        EXTRAS: "twilio,sdk"
        INSTALL_NODE: "true"
        INSTALL_CLAUDE_CLI: "true"
    volumes:
      - ~/.nanobot:/root/.nanobot
      - ~/.claude:/root/.claude
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 1G
        reservations:
          cpus: '0.5'
          memory: 256M

  # Full: all channels + WhatsApp bridge
  nanobot-gateway:
    <<: *common
    profiles:
      - gateway
    build:
      context: .
      args:
        INSTALL_NODE: "true"
        INSTALL_BRIDGE: "true"
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 1G
        reservations:
          cpus: '0.25'
          memory: 256M

  # Full + Claude agent engine
  nanobot-gateway-claude:
    <<: *common
    profiles:
      - gateway-claude
    build:
      context: .
      args:
        INSTALL_NODE: "true"
        INSTALL_BRIDGE: "true"
        INSTALL_CLAUDE_CLI: "true"
    volumes:
      - ~/.nanobot:/root/.nanobot
      - ~/.claude:/root/.claude
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 1G
        reservations:
          cpus: '0.5'
          memory: 256M

  # CLI (interactive)
  nanobot-cli:
    build:
      context: .
    volumes:
      - ~/.nanobot:/root/.nanobot
    profiles:
      - cli
    command: ["status"]
    stdin_open: true
    tty: true
